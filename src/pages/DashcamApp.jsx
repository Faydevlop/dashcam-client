// DashcamApp.jsx
import { useEffect, useRef } from "react";
import { io } from "socket.io-client";

const socket = io("http://localhost:3000"); // replace with your server

const DashcamApp = () => {

};

export default DashcamApp;
